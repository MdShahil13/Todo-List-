# Todo-List-
Flask Todo List with Email Reminder System is a web-based task management application built using Python Flask. The application allows users to create tasks along with a specific date and time. When the scheduled time arrives, the system automatically sends an email notification to the user using SMTP, reminding them about the task. 
